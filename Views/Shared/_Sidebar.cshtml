<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

    <!-- Brand -->
    <a class="sidebar-brand d-flex align-items-center justify-content-center" href="/Home/Index">
        <div class="sidebar-brand-icon rotate-n-15">
            <i class="fas fa-clinic-medical"></i>
        </div>
        <div class="sidebar-brand-text mx-3">CONSULTÓRIO QUALITY</div>
    </a>

    <hr class="sidebar-divider my-0">

    <!-- Dashboard (todos logados) -->
    @if (Session["Perfil"] != null)
    {
        <li class="nav-item">
            <a class="nav-link" href="/Home/Index">
                <i class="fas fa-fw fa-tachometer-alt"></i>
                <span>Dashboard</span>
            </a>
        </li>
    }

    <hr class="sidebar-divider">

    <!-- UTILIZADORES (SÓ ADMIN) -->
    @if (Session["Perfil"]?.ToString() == "Administrador")
    {
        <li class="nav-item">
            <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#menuUtilizadores">
                <i class="fas fa-users-cog"></i>
                <span>Utilizadores</span>
            </a>
            <div id="menuUtilizadores" class="collapse" data-parent="#accordionSidebar">
                <div class="bg-white py-2 collapse-inner rounded">
                    <a class="collapse-item" href="/Utilizadores/Index">Listagem</a>
                    <a class="collapse-item" href="/Utilizadores/Create">Criar Utilizador</a>
                </div>
            </div>
        </li>
    }

    <!-- PACIENTES (ADMIN, MEDICO, RECECIONISTA) -->
    @if (Session["Perfil"] != null)
    {
        <li class="nav-item">
            <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#menuPacientes">
                <i class="fas fa-user-injured"></i>
                <span>Pacientes</span>
            </a>
            <div id="menuPacientes" class="collapse" data-parent="#accordionSidebar">
                <div class="bg-white py-2 collapse-inner rounded">
                    <a class="collapse-item" href="/Pacientes/Index">Listagem</a>
                    <a class="collapse-item" href="/Pacientes/Create">Cadastrar</a>
                </div>
            </div>
        </li>
    }

    <!-- MÉDICOS (SÓ ADMIN) -->
    @if (Session["Perfil"]?.ToString() == "Administrador")
    {
        <li class="nav-item">
            <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#menuMedicos">
                <i class="fas fa-user-md"></i>
                <span>Médicos</span>
            </a>
            <div id="menuMedicos" class="collapse" data-parent="#accordionSidebar">
                <div class="bg-white py-2 collapse-inner rounded">
                    <a class="collapse-item" href="/Medicos/Index">Listagem</a>
                    <a class="collapse-item" href="/Medicos/Create">Cadastrar</a>
                </div>
            </div>
        </li>
    }

    <!-- CONSULTAS (ADMIN, MEDICO, RECECIONISTA) -->
    @if (Session["Perfil"] != null)
    {
        <li class="nav-item">
            <a class="nav-link collapsed" href="#" data-toggle="collapse" data-target="#menuConsultas">
                <i class="fas fa-calendar-check"></i>
                <span>Consultas</span>
            </a>
            <div id="menuConsultas" class="collapse" data-parent="#accordionSidebar">
                <div class="bg-white py-2 collapse-inner rounded">
                    <a class="collapse-item" href="/Consultas/Index">Agenda</a>
                    <a class="collapse-item" href="/Consultas/Create">Marcar Consulta</a>
                </div>
            </div>
        </li>
    }

    <!-- HORÁRIOS (ADMIN + MEDICO) -->
    @if (
        Session["Perfil"]?.ToString() == "Administrador" ||
        Session["Perfil"]?.ToString() == "Medico"
    )
    {
        <li class="nav-item">
            <a class="nav-link collapsed" href="#" data-toggle="collapse"
               data-target="#menuHorarios">
                <i class="fas fa-clock"></i>
                <span>Horários</span>
            </a>

            <div id="menuHorarios" class="collapse" data-parent="#accordionSidebar">
                <div class="bg-white py-2 collapse-inner rounded">
                    <a class="collapse-item" href="/Horarios/Index">Listagem</a>
                    <a class="collapse-item" href="/Horarios/Create">Cadastrar</a>
                </div>
            </div>
        </li>
    }

    <hr class="sidebar-divider d-none d-md-block">

    <div class="text-center d-none d-md-inline">
        <button class="rounded-circle border-0" id="sidebarToggle"></button>
    </div>

</ul>
